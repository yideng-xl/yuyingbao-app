import 'package:flutter/material.dart';

class KnowledgeListPage extends StatelessWidget {
  const KnowledgeListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('知识')),
    );
  }
}
